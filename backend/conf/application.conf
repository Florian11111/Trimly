# https://www.playframework.com/documentation/latest/Configuration
play.http.parser.maxMemoryBuffer = 512k
play.http.parser.maxDiskBuffer = 500MB

# Lange Timeouts für große Dateien und ffmpeg
play.server.http.idleTimeout = 10m
play.server.http.requestTimeout = 10m

# Secret (in Prod via Env-Variable setzen)
play.http.secret.key = ${?APPLICATION_SECRET}
play.http.secret.key = ${?play.http.secret.key}"changeme"

# Hosts (nginx proxied, daher interner Hostname reicht)
play.filters.hosts.allowed = ["."]

play.filters.enabled += "play.filters.cors.CORSFilter"
# In Prod kommt alles über nginx (gleiche Origin), CORS nur für Dev nötig
play.filters.cors.allowedOrigins = ["http://localhost:3000", ${?CORS_ORIGIN}]
play.filters.cors.allowedMethods = ["GET", "POST", "PUT", "DELETE"]
play.filters.cors.allowedHeaders = ["Content-Type", "X-Requested-With", "Accept", "Origin"]
